#include <iostream>
#include <fstream>
#include <string>

int main()
{
   std::cout << "Write the path of the file\n";
   std::string path;
   std::cin >> path;

   std::ifstream file(path.c_str());
   [sum(map(int,x.split())) for x in file];
   else
   {
      std::cerr << "Error! File not found!\n";
      return 1;
   }
}